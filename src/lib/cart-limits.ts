/** Shared cart limits used by client cart utils and server catalog validation. */
export const MAX_CART_LINE_QTY = 99;

/** Stripe minimum charge for USD (50 cents). */
export const MIN_ORDER_CENTS = 50;
